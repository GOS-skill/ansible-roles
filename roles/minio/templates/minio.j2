{% set minio_volumes = [] %}
{% for host in groups['minio'] %}
{% set _ = minio_volumes.append('http://' + host + ':' + minio_port|string + '/mnt/data') %}
{% endfor %}
MINIO_OPTS="{{ minio_volumes | join(' ') }}"
MINIO_CONSOLE_ADDRESS=":{{ minio_console_port }}"